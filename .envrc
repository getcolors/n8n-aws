# -*- mode: sh; sh-shell: bash -*-
export DIRENV_WARN_TIMEOUT=20s
eval "$(devenv direnvrc)"
use devenv 2>/dev/null
source_env_if_exists .envrc.private

# AWS authenticates through the ambient credential chain. The COLORS_PAR_AWS_*
# pair lives in the gitignored .envrc.private and is mapped here; nothing in
# this file is secret.
export AWS_ACCESS_KEY_ID="${COLORS_PAR_AWS_ACCESS_KEY_ID:-}"
export AWS_SECRET_ACCESS_KEY="${COLORS_PAR_AWS_SECRET_ACCESS_KEY:-}"
export AWS_DEFAULT_REGION="${AWS_DEFAULT_REGION:-us-east-1}"
